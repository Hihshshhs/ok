local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local TweenService     = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui          = game:GetService("CoreGui")
local HttpService      = game:GetService("HttpService")
local Lighting         = game:GetService("Lighting")
local Workspace        = game:GetService("Workspace")

local lp = Players.LocalPlayer

local LAG_AMOUNT   = 21.2 * 0.2 / 150
local SPEED_AMOUNT = 33.3

local CFG_FILE   = "bypass_cfg.json"
local lagKeybind = nil
local waitingKey = false

do
    if readfile then
        local ok, raw = pcall(readfile, CFG_FILE)
        if ok and raw and raw ~= "" then
            local ok2, t = pcall(HttpService.JSONDecode, HttpService, raw)
            if ok2 and t and t.lagKey then
                local ok3, kc = pcall(function() return Enum.KeyCode[t.lagKey] end)
                if ok3 and kc then lagKeybind = kc end
            end
        end
    end
end
local function saveCfg()
    if writefile then
        pcall(writefile, CFG_FILE, HttpService:JSONEncode({
            lagKey = lagKeybind and lagKeybind.Name or nil
        }))
    end
end

local lagEnabled = false

local antiLagActive   = false
local antiLagDescConn = nil

local origLighting = {
    GlobalShadows            = Lighting.GlobalShadows,
    FogEnd                   = Lighting.FogEnd,
    Brightness               = Lighting.Brightness,
    EnvironmentDiffuseScale  = Lighting.EnvironmentDiffuseScale,
    EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale,
}
local origEffects = {}
for _, e in pairs(Lighting:GetChildren()) do
    if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect")
    or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then
        origEffects[e] = e.Enabled
    end
end

local origWorkspace = {}
local origSaved     = false

local function saveWorkspaceState()
    if origSaved then return end
    origSaved = true
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail")
        or obj:IsA("Beam") or obj:IsA("Fire") then
            origWorkspace[obj] = { Enabled = obj.Enabled }
        elseif obj:IsA("Decal") or obj:IsA("Texture") then
            origWorkspace[obj] = { Transparency = obj.Transparency }
        elseif obj:IsA("BasePart") then
            origWorkspace[obj] = {
                Material    = obj.Material,
                Reflectance = obj.Reflectance,
                CastShadow  = obj.CastShadow,
            }
        end
    end
end

local function restoreWorkspaceState()
    for obj, props in pairs(origWorkspace) do
        if obj and obj.Parent then
            pcall(function()
                for k, v in pairs(props) do obj[k] = v end
            end)
        end
    end
    origWorkspace = {}
    origSaved     = false
end

local function processObj(obj)
    if obj:IsA("ParticleEmitter") or obj:IsA("Trail")
    or obj:IsA("Beam") or obj:IsA("Fire") then
        pcall(function() obj.Enabled = false end)
    end
    if obj:IsA("Decal") or obj:IsA("Texture") then
        pcall(function() obj.Transparency = 1 end)
    end
    if obj:IsA("BasePart") then
        pcall(function()
            obj.Material    = Enum.Material.Plastic
            obj.Reflectance = 0
            obj.CastShadow  = false
        end)
    end
end

local function watchPartProps(obj)
    if not obj:IsA("BasePart") then return end
    obj:GetPropertyChangedSignal("Material"):Connect(function()
        if antiLagActive and obj.Parent and obj.Material ~= Enum.Material.Plastic then
            pcall(function() obj.Material = Enum.Material.Plastic end)
        end
    end)
    obj:GetPropertyChangedSignal("Reflectance"):Connect(function()
        if antiLagActive and obj.Parent and obj.Reflectance ~= 0 then
            pcall(function() obj.Reflectance = 0 end)
        end
    end)
    obj:GetPropertyChangedSignal("CastShadow"):Connect(function()
        if antiLagActive and obj.Parent and obj.CastShadow then
            pcall(function() obj.CastShadow = false end)
        end
    end)
end

local function doOptimize()
    Lighting.GlobalShadows            = false
    Lighting.FogEnd                   = 9e9
    Lighting.Brightness               = 1
    Lighting.EnvironmentDiffuseScale  = 0
    Lighting.EnvironmentSpecularScale = 0
    for _, e in pairs(Lighting:GetChildren()) do
        if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect")
        or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then
            e.Enabled = false
        end
    end
    for _, obj in pairs(Workspace:GetDescendants()) do processObj(obj) end
end

local function enableAntiLag()
    if antiLagActive then return end
    saveWorkspaceState()
    antiLagActive = true
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
    pcall(function() doOptimize() end)
    for _, obj in pairs(Workspace:GetDescendants()) do watchPartProps(obj) end
    if antiLagDescConn then antiLagDescConn:Disconnect() end
    antiLagDescConn = Workspace.DescendantAdded:Connect(function(obj)
        if antiLagActive then
            task.defer(function()
                if obj and obj.Parent and antiLagActive then
                    pcall(function() processObj(obj) end)
                    watchPartProps(obj)
                end
            end)
        end
    end)
end

task.spawn(enableAntiLag)

local COL_BG     = Color3.fromRGB(12, 16, 35)
local COL_ROW    = Color3.fromRGB(20, 25, 50)
local COL_TOGGLE = Color3.fromRGB(30, 40, 70)
local COL_BLUE   = Color3.fromRGB(0, 120, 255)
local MIN_NORMAL = Color3.fromRGB(30, 40, 70)
local MIN_HOVER  = Color3.fromRGB(16, 22, 42)

local STROKE_CS = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(0,  20,  60)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(0, 160, 255)),
    ColorSequenceKeypoint.new(0.4, Color3.new(1,1,1)),
    ColorSequenceKeypoint.new(0.6, Color3.new(1,1,1)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(0, 160, 255)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(0,  20,  60)),
})

local allGrads = {}
local function animStroke(parent, thick)
    local s = Instance.new("UIStroke")
    s.Thickness = thick or 1
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.new(1,1,1)
    s.Parent = parent
    local g = Instance.new("UIGradient")
    g.Color = STROKE_CS; g.Rotation = 45; g.Parent = s
    table.insert(allGrads, g)
    return s
end

if not CoreGui:FindFirstChild("HiddenUI") then
    Instance.new("Folder", CoreGui).Name = "HiddenUI"
end
if CoreGui.HiddenUI:FindFirstChild("IceHub_Bypass") then
    CoreGui.HiddenUI.IceHub_Bypass:Destroy()
end

local gui = Instance.new("ScreenGui")
gui.Name = "IceHub_Bypass"; gui.ResetOnSpawn = false
gui.Parent = CoreGui.HiddenUI

local FULL_H = 88
local MINI_H = 42

local main = Instance.new("Frame")
main.Size                   = UDim2.new(0, 210, 0, FULL_H)
main.Position               = UDim2.new(0.5, -105, 0.5, -FULL_H//2)
main.BackgroundColor3       = COL_BG
main.BackgroundTransparency = 0.4
main.BorderSizePixel        = 0
main.ClipsDescendants       = true
main.Active                 = true
main.Parent                 = gui
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 10)
animStroke(main, 2)

local title = Instance.new("TextLabel")
title.Size               = UDim2.new(1, -40, 0, 16)
title.Position           = UDim2.new(0, 10, 0, 4)
title.BackgroundTransparency = 1
title.Text               = "Ice Hub - Bypass"
title.Font               = Enum.Font.GothamBlack
title.TextSize           = 14
title.TextColor3         = Color3.new(1,1,1)
title.TextXAlignment     = Enum.TextXAlignment.Left
title.Parent             = main
local tGrad = Instance.new("UIGradient")
tGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(0, 120, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.new(1,1,1)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 120, 255)),
})
tGrad.Parent = title

local desc = Instance.new("TextLabel")
desc.Size               = UDim2.new(1, -40, 0, 10)
desc.Position           = UDim2.new(0, 10, 0, 22)
desc.BackgroundTransparency = 1
desc.Text               = "discord.gg/icehub"
desc.Font               = Enum.Font.Gotham
desc.TextSize           = 9
desc.TextColor3         = Color3.fromRGB(70, 100, 170)
desc.TextXAlignment     = Enum.TextXAlignment.Left
desc.Parent             = main

local minimized = false
local minBtn = Instance.new("TextButton")
minBtn.Size             = UDim2.new(0, 24, 0, 24)
minBtn.Position         = UDim2.new(1, -32, 0, 8)
minBtn.BackgroundColor3 = MIN_NORMAL
minBtn.Text             = "-"
minBtn.TextColor3       = Color3.new(1,1,1)
minBtn.Font             = Enum.Font.GothamBlack
minBtn.TextSize         = 14
minBtn.BorderSizePixel  = 0
minBtn.AutoButtonColor  = false
minBtn.Parent           = main
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 6)
animStroke(minBtn, 1)

local minScale = Instance.new("UIScale")
minScale.Scale  = 1
minScale.Parent = minBtn

minBtn.MouseEnter:Connect(function()
    TweenService:Create(minBtn, TweenInfo.new(0.15), {
        BackgroundColor3 = MIN_HOVER
    }):Play()
end)
minBtn.MouseLeave:Connect(function()
    TweenService:Create(minBtn, TweenInfo.new(0.15), {
        BackgroundColor3 = MIN_NORMAL
    }):Play()
end)
minBtn.MouseButton1Down:Connect(function()
    TweenService:Create(minBtn, TweenInfo.new(0.05), {
        BackgroundColor3 = Color3.fromRGB(5, 7, 18)
    }):Play()
    TweenService:Create(minScale, TweenInfo.new(0.07, Enum.EasingStyle.Linear), {
        Scale = 0.93
    }):Play()
    task.delay(0.07, function()
        TweenService:Create(minBtn, TweenInfo.new(0.18), {
            BackgroundColor3 = MIN_NORMAL
        }):Play()
        TweenService:Create(minScale, TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Scale = 1
        }):Play()
    end)
end)
minBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    TweenService:Create(main, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {
        Size = minimized and UDim2.new(0,210,0,MINI_H) or UDim2.new(0,210,0,FULL_H)
    }):Play()
    minBtn.Text = minimized and "+" or "-"
end)

local row = Instance.new("Frame")
row.Size                   = UDim2.new(1, -20, 0, 34)
row.Position               = UDim2.new(0, 10, 0, 48)
row.BackgroundColor3       = COL_ROW
row.BackgroundTransparency = 0.55
row.BorderSizePixel        = 0
row.Parent                 = main
Instance.new("UICorner", row)
animStroke(row, 1)

local lbl = Instance.new("TextLabel")
lbl.Size             = UDim2.new(1, -100, 1, 0)
lbl.Position         = UDim2.new(0, 10, 0, 0)
lbl.BackgroundTransparency = 1
lbl.Text             = "Bypass"
lbl.Font             = Enum.Font.GothamBlack
lbl.TextSize         = 13
lbl.TextColor3       = Color3.new(1,1,1)
lbl.TextXAlignment   = Enum.TextXAlignment.Left
lbl.Parent           = row

local lagKbBtn = Instance.new("TextButton")
lagKbBtn.Size             = UDim2.new(0, 35, 0, 18)
lagKbBtn.Position         = UDim2.new(1, -90, 0.5, -9)
lagKbBtn.BackgroundColor3 = COL_TOGGLE
lagKbBtn.Text             = lagKeybind and "["..lagKeybind.Name.."]" or "[...]"
lagKbBtn.TextColor3       = Color3.new(1,1,1)
lagKbBtn.Font             = Enum.Font.GothamBold
lagKbBtn.TextSize         = 10
lagKbBtn.BorderSizePixel  = 0
lagKbBtn.AutoButtonColor  = false
lagKbBtn.Parent           = row
Instance.new("UICorner", lagKbBtn).CornerRadius = UDim.new(0, 4)

local lagTrack = Instance.new("Frame")
lagTrack.Size             = UDim2.new(0, 36, 0, 18)
lagTrack.Position         = UDim2.new(1, -46, 0.5, -9)
lagTrack.BackgroundColor3 = COL_TOGGLE
lagTrack.BorderSizePixel  = 0
lagTrack.Parent           = row
Instance.new("UICorner", lagTrack).CornerRadius = UDim.new(0, 9)
animStroke(lagTrack, 1)

local lagKnob = Instance.new("Frame")
lagKnob.Size             = UDim2.new(0, 14, 0, 14)
lagKnob.Position         = UDim2.new(0, 2, 0.5, -7)
lagKnob.BackgroundColor3 = Color3.new(1,1,1)
lagKnob.BorderSizePixel  = 0
lagKnob.Parent           = lagTrack
Instance.new("UICorner", lagKnob).CornerRadius = UDim.new(0, 7)

local lagBtn = Instance.new("TextButton")
lagBtn.Size               = UDim2.new(0, 46, 1, 0)
lagBtn.Position           = UDim2.new(1, -46, 0, 0)
lagBtn.BackgroundTransparency = 1
lagBtn.Text               = ""
lagBtn.Parent             = row

local function setLag(state)
    lagEnabled = state
    TweenService:Create(lagKnob, TweenInfo.new(0.15), {
        Position = state and UDim2.new(1,-16,0.5,-7) or UDim2.new(0,2,0.5,-7)
    }):Play()
    TweenService:Create(lagTrack, TweenInfo.new(0.15), {
        BackgroundColor3 = state and COL_BLUE or COL_TOGGLE
    }):Play()
end

lagBtn.MouseButton1Click:Connect(function() setLag(not lagEnabled) end)
lagKbBtn.MouseButton1Click:Connect(function()
    waitingKey    = true
    lagKbBtn.Text = "[...]"
end)

do
    local dragging, dragStart, startPos = false, nil, nil
    main.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then
            local abs = main.AbsolutePosition
            main.Position = UDim2.new(0, abs.X, 0, abs.Y)
            dragging  = true
            dragStart = i.Position
            startPos  = main.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if not dragging then return end
        if i.UserInputType == Enum.UserInputType.MouseMovement
        or i.UserInputType == Enum.UserInputType.Touch then
            local d  = i.Position - dragStart
            local vp = workspace.CurrentCamera.ViewportSize
            local gs = main.AbsoluteSize
            main.Position = UDim2.new(0,
                math.clamp(startPos.X.Offset+d.X, 0, vp.X-gs.X), 0,
                math.clamp(startPos.Y.Offset+d.Y, 0, vp.Y-gs.Y))
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if waitingKey then
        if input.UserInputType == Enum.UserInputType.Keyboard
        and input.KeyCode ~= Enum.KeyCode.Unknown then
            lagKeybind    = input.KeyCode
            lagKbBtn.Text = "[" .. lagKeybind.Name .. "]"
            waitingKey    = false
            saveCfg()
        end
        return
    end
    if lagKeybind and input.KeyCode == lagKeybind then
        setLag(not lagEnabled)
    end
end)

RunService.RenderStepped:Connect(function()
    local off = Vector2.new(math.sin(tick() * 2.8), 0)
    for _, g in ipairs(allGrads) do g.Offset = off end
end)

RunService.RenderStepped:Connect(function()
    if not lagEnabled then return end
    local char = lp.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then return end
    if hum.MoveDirection.Magnitude > 0 then
        hrp.Velocity = Vector3.new(
            hum.MoveDirection.X * SPEED_AMOUNT,
            hrp.Velocity.Y,
            hum.MoveDirection.Z * SPEED_AMOUNT)
    end
end)

RunService.RenderStepped:Connect(function()
    if not lagEnabled or LAG_AMOUNT <= 0 then return end
    local t = tick()
    while tick() - t < LAG_AMOUNT do end
end)